LAST_TAG=$(git describe) #sono nella cartella prova
read -p ${LAST_TAG}
BETA=$(echo $LAST_TAG | cut -d'.' -f 4)
if [ $BETA != "beta" ]  #ero fuori dalla beta
then
	echo "non sono in beta"
else
	echo "sono in beta"
fi
read -p "Press enter to continue"